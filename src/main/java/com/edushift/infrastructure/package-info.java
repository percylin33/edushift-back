/**
 * Technical infrastructure adapters (persistence, cache, messaging, external APIs).
 * <p>
 * Implements ports defined in {@code shared} or module layers; modules depend on abstractions,
 * not concrete infrastructure where possible.
 */
package com.edushift.infrastructure;
