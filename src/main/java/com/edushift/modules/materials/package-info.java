/**
 * LMS materials module (Sprint 7a / BE-7a.1).
 *
 * <p>Owns the {@code lms_materials} table and exposes the per-section
 * resource library endpoints. A material is either an uploaded binary
 * (re-uses the {@code files} shared kernel) or an external video link.
 *
 * <p>Source of truth: {@code docs/modules/materials.md}.
 */
package com.edushift.modules.materials;
