/**
 * Cross-cutting audit config (Sprint 15 / F-07 / H-03).
 * <p>Reserved for future CORS, paging default overrides, etc. — left
 * empty for now so the package is wired into component scanning.
 */
package com.edushift.modules.audit.config;
