/**
 * Implementations for the auth module's service interfaces.
 * <p>
 * Kept in a separate {@code impl} package so the public API
 * ({@code AuthService}, {@code JwtService}) stays clearly distinguishable
 * from its internals.
 */
package com.edushift.modules.auth.service.impl;
